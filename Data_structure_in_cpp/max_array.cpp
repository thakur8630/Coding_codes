#include<bits/stdc++.h>
using namespace std;
int max_array(int arr[], int n){
	int max=arr[0];
	for(int i=0;i<n;i++)
		cout<<"enter the elements";
		cin>>arr[i];
		if(arr[i]>max)
			max=arr[i]
			
			
		return max;
		
			
			
			
			
		
		
	
}
int main(){
	cout<<"enter the range";
	cin>>n;
	cout<<"the maximum element of the givin array is...  "<<max_array(arr,n);
	return 0;
	
}
